import UIKit

extension UIColor {

    enum Weather {
        static let gradientTop = UIColor(red: 0.40, green: 0.69, blue: 0.94, alpha: 1.0)
        static let gradientBottom = UIColor(red: 0.18, green: 0.35, blue: 0.72, alpha: 1.0)
        static let cardBackground = UIColor.white.withAlphaComponent(0.15)
        static let cardBorder = UIColor.white.withAlphaComponent(0.25)
        static let primaryText = UIColor.white
        static let secondaryText = UIColor.white.withAlphaComponent(0.7)
        static let separator = UIColor.white.withAlphaComponent(0.2)
    }
}
